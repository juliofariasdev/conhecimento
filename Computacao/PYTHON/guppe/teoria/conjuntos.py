"""
Conjuntos (sets):

set é um conjunto matemático.

- Sets não possuem valores duplicados;
- Sets não possuem valores ordenados;
- Elementos não possuem índice.

Conjuntos são coleção do tipo valor.

São representados por chave {}.

conjunto.add(elemento) -> adiciona o elemento a lista

conjunto.remove(elemento) -> remove o elemento da lista

conjunto.discard(elemento) -> discarda o elemento da lista

conjunto1.union(conjunto2) -> faz a união entre o conjunto1 e o conjunto2

conjuto1.intersection(conjunto2) -> faz interseção entre o conjunto1 e o conjunto2

conjunto1.difference(conjunto2) -> faz a diferença (conjunto1 - conjunto2)

"""